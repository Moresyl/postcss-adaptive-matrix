{
  "description": "A theme token naming a text property gets the same zoom-safe output a real declaration would",
  "options": {
    "defaultProfile": "app",
    "profiles": {
      "app": { "designWidth": 750, "fluid": { "minWidth": 320, "maxWidth": 600 } }
    },
    "libraries": ["vant"]
  }
}
